{
    admin off
}

:{$PORT} {
    root * .

    # ส่ง Request ขึ้นต้นด้วย /api ไปที่ PHP-FPM โดยตรง
    handle /api/* {
        php_fastcgi localhost:9000
    }

    # บริการ Static Files ทั่วไป
    handle {
        file_server
    }
}
